/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.unsplash.com', 'i.redd.it', 'steamuserimages-a.akamaihd.net'],
    },
};

export default nextConfig;
